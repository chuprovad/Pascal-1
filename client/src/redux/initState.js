const initialState = () => {
  const state = {
    userInfo: null,
    restaurant: {},
    rests: [],
    reservation: {},
    searchInput: '',
    incident: []
  };

  return state;
};

export default initialState;
